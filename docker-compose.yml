version: '3.8'

services:
  evosense-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: evosensefleet-api
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOST=0.0.0.0
      - DATABASE_PATH=/app/data/evosensefleet.db
    volumes:
      - evosense_data:/app/data

volumes:
  evosense_data:
