{
  "name": "@evosensefleet/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "dev:node": "tsx src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc -p tsconfig.json && esbuild src/server.ts --bundle --platform=node --format=esm --target=node22 --outfile=dist/server.js --packages=external --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute evosensefleet-db --file=./migrations/0001_app_state.sql --local && wrangler d1 execute evosensefleet-db --file=./migrations/0002_normalized_tables.sql --local",
    "db:migrate:remote": "wrangler d1 execute evosensefleet-db --file=./migrations/0001_app_state.sql --remote && wrangler d1 execute evosensefleet-db --file=./migrations/0002_normalized_tables.sql --remote"
  },
  "dependencies": {
    "@evosensefleet/shared": "^1.0.0",
    "@hono/node-server": "^1.13.7",
    "dotenv": "^16.4.7",
    "hono": "^4.5.0",
    "mysql2": "^3.12.0",
    "vite": "8.1.4",
    "@noble/hashes": "^1.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "esbuild": "^0.28.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "wrangler": "^4.107.1"
  }
}
